{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.selflabbs/tokenbrake",
  "description": "Let an AI agent read its own API spend and hard-stop its own runaway bill at your budget.",
  "repository": {
    "url": "https://github.com/selflabbs/tokenbrake-mcp",
    "source": "github"
  },
  "version": "1.0.0",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "tokenbrake-mcp",
      "version": "1.0.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "TB_URL",
          "description": "Base URL of your running TokenBrake server. Defaults to http://localhost:8788",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "TB_KEY",
          "description": "Your TokenBrake server key, if you set one. Sent as the x-tokenbrake-key header.",
          "isRequired": false,
          "format": "string",
          "isSecret": true
        }
      ]
    }
  ]
}
